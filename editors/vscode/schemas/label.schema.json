{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://fleetdm.com/schemas/label.json",
  "title": "Fleet Label",
  "description": "Schema for Fleet label configuration",
  "type": "object",
  "required": ["name", "label_membership_type"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Label name (identifier)",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "Label description"
    },
    "platform": {
      "type": "string",
      "description": "Target platform",
      "enum": ["darwin", "windows", "ubuntu", "centos", "linux", "chrome", ""]
    },
    "label_membership_type": {
      "type": "string",
      "description": "Type of label membership",
      "enum": ["dynamic", "manual", "host_vitals"]
    },
    "query": {
      "type": "string",
      "description": "SQL query for dynamic labels (required when label_membership_type is 'dynamic')"
    },
    "hosts": {
      "type": "array",
      "description": "Host IDs for manual labels (required when label_membership_type is 'manual')",
      "items": {
        "type": "integer"
      }
    },
    "criteria": {
      "$ref": "#/$defs/criteria",
      "description": "Criteria for host_vitals labels (required when label_membership_type is 'host_vitals')"
    }
  },
  "$defs": {
    "criteria": {
      "type": "object",
      "properties": {
        "vital": {
          "type": "string",
          "description": "Host vital identifier (e.g. os_version, os_name, os_arch)"
        },
        "value": {
          "description": "Expected vital value"
        },
        "operator": {
          "type": "string",
          "description": "Comparison operator",
          "enum": ["==", "!=", ">", ">=", "<", "<="]
        },
        "and": {
          "type": "array",
          "description": "All nested criteria must match",
          "items": { "$ref": "#/$defs/criteria" }
        },
        "or": {
          "type": "array",
          "description": "Any nested criteria must match",
          "items": { "$ref": "#/$defs/criteria" }
        }
      },
      "additionalProperties": false
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "label_membership_type": {
            "const": "dynamic"
          }
        }
      },
      "then": {
        "required": ["query"]
      }
    },
    {
      "if": {
        "properties": {
          "label_membership_type": {
            "const": "manual"
          }
        }
      },
      "then": {
        "required": ["hosts"]
      }
    },
    {
      "if": {
        "properties": {
          "label_membership_type": {
            "const": "host_vitals"
          }
        }
      },
      "then": {
        "required": ["criteria"]
      }
    }
  ],
  "additionalProperties": false
}
