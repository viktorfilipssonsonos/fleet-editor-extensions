[package]
name = "flint-zed"
version = "0.1.3"
edition = "2021"
description = "Zed extension for Flint — Fleet GitOps YAML linting"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
